git init
git status
git add nombreArchivo
git add -A  (agregar todos los archivos del directorio)
git rm --cached nombreArchivo (retirar archivo del index)
git log (ver commits)
git commit -m "nombre del commit"
git commit checkout codigoCommit
git reset --soft codigoCommit
git reset --hard codigoCommit

git branch "nombrenuevarama"
git checkout "nombrerama" (pasarse a otra rama)
git merge

https://github.com/Daniela-git/CodigoFacilito.git